#include <stdio.h>
#include <string.h>

int main(){
	int n;
	char a[200];
	scanf("%d",&n);
	for(int i=0;i<n;i++){
		getchar();
		fgets(a,200,stdin);
		int len;
		len=strlen(a)-1;
		for(int j=0;j<len;j+=2){
			if(a[j]%2!=0)
			b[]
		}
	}
}
